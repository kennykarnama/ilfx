#ifndef ILFX_ALLHEADERS_HPP
#define ILFX_ALLHEADERS_HPP

// standard library headers
#include <string>
#include <memory>
#include <iostream>

// Operation status definitions
enum OperationStatus
{
    SuccessOperationStatus = 0,
    FailureOperationStatus = 1,
    // add other data types here
};

#endif // ILFX_ALLHEADERS_HPP